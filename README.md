# Ononoki - Mobile Manga Reader (React Native)

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start or npx expo run:android
   ```

# APK
- eas build -p android --profile preview
